"""
Модуль для инициализации базы данных.
Инициализация теперь выполняется в app.py при запуске приложения.
Этот файл оставлен для совместимости, но не используется напрямую.
"""

from models import db, Group, Server, Printer, Camera, Router

def init_database():
    """
    Инициализация базы данных с тестовыми данными.
    
    Примечание: Эта функция больше не используется напрямую.
    Инициализация выполняется автоматически в app.py при запуске приложения.
    """
    # Функция оставлена для обратной совместимости
    # Реальная инициализация происходит в app.py
    pass